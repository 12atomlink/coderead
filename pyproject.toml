[project]
name = "coderead"
version = "0.1.0"
description = "Make any code repository understandable to humans"
requires-python = ">=3.13"
dependencies = [
    "httpx[socks]>=0.28.1",
    "openai>=2.32.0",
]
